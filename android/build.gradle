buildscript {
    ext {
        buildToolsVersion = "35.0.0"
        minSdkVersion = 24
        compileSdkVersion = 35
        targetSdkVersion = 34
        ndkVersion = "26.1.10909125"
        kotlinVersion = "1.8.22"   // enforced
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:${kotlinVersion}")
    }
}

apply plugin: "com.facebook.react.rootproject"

// Force all Kotlin compile tasks to use JVM 17 and compatibility flags
subprojects {
    tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).all {
        kotlinOptions {
            jvmTarget = "17"
            freeCompilerArgs += [
                "-Xskip-prerelease-check",
                "-Xallow-unstable-dependencies"
            ]
        }
    }
}

// Global resolution: pin every Kotlin dependency to 1.8.22
configurations.all {
    resolutionStrategy {
        eachDependency { DependencyResolveDetails details ->
            if (details.requested.group == 'org.jetbrains.kotlin') {
                details.useVersion kotlinVersion
            }
            if (details.requested.group == 'org.jetbrains.kotlinx') {
                // Keep kotlinx coroutines at a compatible version (1.6.4 works with 1.8.22)
                if (details.requested.name == 'kotlinx-coroutines-core' ||
                    details.requested.name == 'kotlinx-coroutines-android') {
                    details.useVersion '1.6.4'
                }
            }
        }
    }
}